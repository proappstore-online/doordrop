// TODO: port from the original — uses currentUser without null checks.
// Will revisit when porting the walker dashboard cluster.
export default function WalkerHistoryPage() {
  return (
    <div className="p-6">
      <h1 className="text-xl font-semibold">History</h1>
      <p className="text-gray-500 dark:text-gray-400 mt-2">Coming soon.</p>
    </div>
  );
}
